# HC4 ユーザーズマニュアル ソフトウェア編

- [HC4 ユーザーズマニュアル ソフトウェア編](#hc4-ユーザーズマニュアル-ソフトウェア編)
  - [表の読み方](#表の読み方)
  - [命令テーブル](#命令テーブル)
    - [HC4](#hc4)
    - [HC4E](#hc4e)
    - [HC8](#hc8)
    - [総合](#総合)
  - [命令詳細](#命令詳細)
    - [論理演算・算術演算](#論理演算算術演算)
      - [`SU`](#su)
      - [`AD`](#ad)
      - [`XR`](#xr)
      - [`OR`](#or)
      - [`AN`](#an)
    - [レジスタ・メモリアクセス](#レジスタメモリアクセス)
      - [`SM`](#sm)
      - [`SC r`](#sc-r)
      - [`SA r`](#sa-r)
      - [`LM`](#lm)
      - [`LD`](#ld)
      - [`LI`](#li)
    - [システム制御](#システム制御)
      - [`JP`, `NP`](#jp-np)
  - [注意すべき点](#注意すべき点)
    - [HC4E の注意](#hc4e-の注意)
    - [SA r / SC r / SM](#sa-r--sc-r--sm)
    - [LD r / LI #i / LM](#ld-r--li-i--lm)
    - [JP 系 / NP](#jp-系--np)
  - [チュートリアル: LED点滅（lchika.asm）](#チュートリアル-led点滅lchikaasm)
  - [トラブルシューティング](#トラブルシューティング)

## 表の読み方

HC4シリーズの命令幅は 8 bit です。

- `r`: 4 bit レジスタ番号（`r0` から `r15`）
- `A`, `B`, `C`: スタックA/B/C
- `#i`: 4 bit 即値
- `[AB]`: スタック A/B を連結した間接アドレス（MSB は B）
- `[ABC]`: スタック A/B/C を連結した間接アドレス（MSB は C）
- `PC`: Program Counter

（Reserved）は未定義命令です。動作は保証されません。

## 命令テーブル

### HC4

| bit 5-4→<br>7-6↓ | 00         | 01         | 10                      | 11         |
| ---------------- | ---------- | ---------- | ----------------------- | ---------- |
| 00               | `SM`       | `SC r`     | `SU r`                  | `AD r`     |
| 01               | `XR r`     | `OR r`     | `AN r`                  | `SA r`     |
| 10               | `LM`       | `LD r`     | `LI #`                  | (Reserved) |
| 11               | (Reserved) | (Reserved) | `JP flag`, `NP`         | (Reserved) |

### HC4E

| bit 5-4→<br>7-6↓ | 00         | 01         | 10                      | 11         |
| ---------------- | ---------- | ---------- | ----------------------- | ---------- |
| 00               | (Reserved) | (Reserved) | (Reserved)              | `AD r`     |
| 01               | `XR r`     | (Reserved) | (Reserved)              | `SA r`     |
| 10               | (Reserved) | `LD r`     | `LI #`                  | (Reserved) |
| 11               | (Reserved) | (Reserved) | `JP flag`, `NP`         | (Reserved) |

### HC8

| bit 5-4→<br>7-6↓ | 00         | 01         | 10                      | 11                      |
| ---------------- | ---------- | ---------- | ----------------------- | ----------------------- |
| 00               | `SM`       | `SC r`     | `SU r`                  | `AD r`                  |
| 01               | `XR r`     | `OR r`     | `AN r`                  | `SA r`                  |
| 10               | `LM`       | `LD r`     | `LI #`                  | (Reserved)              |
| 11               | `LS #`     | (Reserved) | `JP flag`, `NP`         | `JL flag`, `LP`         |

### 総合

| 命令     | Opc  | Opr  | フラグ変化 | 動作      | 説明                   |
|----------|------|------|---------|-----------|------------------------|
| `SU r`   | 0010 | r    | C, Z    | r <= A-B  | 減算結果を r に格納      |
| `AD r`   | 0011 | r    | C, Z    | r <= A+B  | 加算結果を r に格納      |
| `XR r`   | 0100 | r    | Z       | r <= A^B  | XOR 結果を r に格納※1      |
| `XR r`   | 0100 | r    | Z       | r <= ~(A&B)  | NAND 結果を r に格納※2      |
| `OR r`   | 0101 | r    | Z       | r <= A\|B | OR 結果を r に格納       |
| `AN r`   | 0110 | r    | Z       | r <= A&B  | AND 結果を r に格納      |
| `SM`     | 0000 | 0000 | Z       |           | C を `[AB]` に格納       |
| `SC r`   | 0001 | r    | Z       |           | C を r に格納            |
| `SA r`   | 0111 | r    | Z       |           | A を r に格納            |
| `LM`     | 1000 | 0000 |         |           | `[AB]` から A にロード       |
| `LD r`   | 1001 | r    |         |           | r から A にロード            |
| `LI #i`  | 1010 | i    |         |           | 即値を A にロード           |
| `LS #i`* | 1100 | i    |         |           | 即値ロード + A シフト（HC8のみ） |
| `JP`     | 1110 | 0000 |         |           | `[ABC]` へジャンプ          |
| `JP C`   | 1110 | 0010 |         |           | C フラグが 1 のときジャンプ       |
| `JP NC`  | 1110 | 0011 |         |           | C フラグが 0 のときジャンプ       |
| `JP Z`   | 1110 | 0100 |         |           | Z フラグが 1 のときジャンプ       |
| `JP NZ`  | 1110 | 0101 |         |           | Z フラグが 0 のときジャンプ       |
| `NP`     | 1110 | 0001 |         |           | 何もしない                 |

- ※1 HC4e以外
- ※2 HC4eのみ。ニーモニックは暫定。

## 命令詳細

### 論理演算・算術演算

| 命令   | Opc  | Opr | フラグ変化 | 動作         | 説明                   |
| ------ | ---- | --- | ---------- | ----------- | --------------------- |
| `SU r` | 0010 | r   | C, Z       | r <= A-B    | 減算結果を r に格納     |
| `AD r` | 0011 | r   | C, Z       | r <= A+B    | 加算結果を r に格納     |
| `XR r` | 0100 | r   | Z          | r <= A^B    | XOR 結果を r に格納※1  |
| `XR r` | 0100 | r   | Z          | r <= ~(A&B) | NAND 結果を r に格納※2 |
| `OR r` | 0101 | r   | Z          | r <= A\|B   | OR 結果を r に格納      |
| `AN r` | 0110 | r   | Z          | r <= A&B    | AND 結果を r に格納     |

これらの命令はスタック レベル`A`と`B`を入力に取り、演算結果を指定レジスタ r に格納します。

#### `SU`

| 命令   | Opc  | Opr | フラグ変化 | 動作       | 説明                   |
| ------ | ---- | --- | ---------- | ---------- | ---------------------- |
| `SU r` | 0010 | r   | C, Z       | r <= A-B   | 減算結果を r に格納    |

例:

```asm
LI #3
; stack A B C
;       3 x x
LI #5
; stack A B C
;       5 3 x
SU r0
; r0(0x00) <= 2 (5-3)
```

- 副作用
  - フラグ
    - `C`フラグ:もし結果が正なら、`1`(負なら`0`)
    - `Z`フラグ:もし結果が0なら、`1`(0でないなら`0`)

#### `AD`

| 命令   | Opc  | Opr | フラグ変化 | 動作       | 説明                   |
| ------ | ---- | --- | ---------- | ---------- | ---------------------- |
| `AD r` | 0011 | r   | C, Z       | r <= A+B   | 加算結果を r に格納    |

例:

```asm
LI #3
; stack A B C
;       3 x x
LI #5
; stack A B C
;       5 3 x
AD r0
; r0(0x00) <= 8 (5+3)
```

- 副作用
  - フラグ
    - `C`フラグ:結果が16以上なら、`1`(16未満なら`0`)
    - `Z`フラグ:もし結果が0なら、`1`(0でないなら`0`)

#### `XR`

HC4e以外

| 命令   | Opc  | Opr | フラグ変化 | 動作       | 説明                   |
| ------ | ---- | --- | ---------- | ---------- | ---------------------- |
| `XR r` | 0100 | r   | C, Z       | r <= A^B   | XOR結果を r に格納    |

HC4e

| 命令   | Opc  | Opr | フラグ変化 | 動作       | 説明                   |
| ------ | ---- | --- | ---------- | ---------- | ---------------------- |
| `XR r` | 0100 | r   | C, Z       | r <= ~(A&B)   | NAND結果を r に格納    |

命令名は暫定のものです。

例:

```asm
LI #0b0011
; stack A B C
;       3 x x
LI #0b0101
; stack A B C
;       5 3 x
XR r0
; r0(0x00) <= 0b0110 (0b0011 xor 0b0101)
```

- 副作用
  - フラグ
    - `C`フラグ:影響しません。
    - `Z`フラグ:もし結果が0なら、`1`(0でないなら`0`)

#### `OR`

| 命令   | Opc  | Opr | フラグ変化 | 動作       | 説明                   |
| ------ | ---- | --- | ---------- | ---------- | ---------------------- |
| `OR r` | 0101 | r   | C, Z       | r <= A|B   | OR結果を r に格納    |

例:

```asm
LI #0b0011
; stack A B C
;       3 x x
LI #0b0101
; stack A B C
;       5 3 x
OR r0
; r0(0x00) <= 0b0111 (0b0011 or 0b0101)
```

- 副作用
  - フラグ
    - `C`フラグ:影響しません。
    - `Z`フラグ:もし結果が0なら、`1`(0でないなら`0`)

#### `AN`

| 命令   | Opc  | Opr | フラグ変化 | 動作       | 説明                   |
| ------ | ---- | --- | ---------- | ---------- | ---------------------- |
| `AN r` | 0110 | r   | Z          | r <= A&B   | AND 結果を r に格納    |

例:

```asm
LI #0b0011
; stack A B C
;       3 x x
LI #0b0101
; stack A B C
;       5 3 x
AN r0
; r0(0x00) <= 0b0001 (0b0011 and 0b0101)
```

- 副作用
  - フラグ
    - `C`フラグ:影響しません。
    - `Z`フラグ:もし結果が0なら、`1`(0でないなら`0`)

### レジスタ・メモリアクセス

| 命令     | Opc  | Opr  | フラグ変化 | 説明                               |
| -------- | ---- | ---- | ---------- | ---------------------------------- |
| `SM`     | 0000 | 0000 | Z          | C を `[AB]` に格納                 |
| `SC r`   | 0001 | r    | Z          | C を r に格納                      |
| `SA r`   | 0111 | r    | Z          | A を r に格納                      |
| `LM`     | 1000 | 0000 |            | `[AB]` から A にロード             |
| `LD r`   | 1001 | r    |            | r から A にロード                  |
| `LI #i`  | 1010 | i    |            | 即値を A にロード                  |
| `LS #i`* | 1100 | i    |            | 即値ロード + A シフト（HC8のみ）   |

注記:

- `LS #i` は HC8 のみ。
- Store 系命令は A または C をレジスタ/メモリへ保存します。
  - `SA r`: A を参照
  - `SC r`, `SM`: C を参照
- Load 系命令は A に値を読み込みます。
  - ロード時は A/B/C が 1 段ずつ押し下がります（A <- new, B <- old A, C <- old B）。
  - `LD r`: レジスタ参照
  - `LI #i`: 即値参照
  - `LM`: `[AB]` で指定されたメモリアドレス参照
- `LS #i` と `LI #i` では 2進リテラル（例: `0b1010`）と 16進リテラル（例: `0xA`）を使用できます。

#### `SM`

| 命令     | Opc  | Opr  | フラグ変化 | 説明                               |
| -------- | ---- | ---- | ---------- | ---------------------------------- |
| `SM`     | 0000 | 0000 | Z          | C を `[AB]` に格納                 |

スタック レベル`C`の値をスタック レベル`A`およびBであらわされるアドレスに格納します。アドレスのMSBはレベル`B`です。

例 :

```asm
LI #3
; stack A B C
;       3 x x
LI #0xF
; stack A B C
;       F 3 x
LI #0x0
; stack A B C
;       0 F 3
SM
; stores 3 to address 0xF0 
```

- 副作用
  - フラグ
    - `C`フラグ:影響しません。
    - `Z`フラグ:もし結果が0なら、`1`(0でないなら`0`)

#### `SC r`

| 命令     | Opc  | Opr  | フラグ変化 | 説明                               |
| -------- | ---- | ---- | ---------- | ---------------------------------- |
| `SC r`   | 0001 | r    | Z          | C を r に格納                      |

スタック レベル`C`の値をrであらわされるアドレスに格納します。

#### `SA r`

| 命令     | Opc  | Opr  | フラグ変化 | 説明                               |
| -------- | ---- | ---- | ---------- | ---------------------------------- |
| `SA r`   | 0111 | r    | Z          | A を r に格納                      |

スタック レベル`A`の値をrであらわされるアドレスに格納します。

例 :

```asm
LI #3
; stack A B C
;       3 x x
SA r3
; stores 3 to 0x03(r3)
```

- 副作用
  - フラグ
    - `C`フラグ:影響しません。
    - `Z`フラグ:もし結果が0なら、`1`(0でないなら`0`)

#### `LM`

| 命令     | Opc  | Opr  | フラグ変化 | 説明                               |
| -------- | ---- | ---- | ---------- | ---------------------------------- |
| `LM`     | 1000 | 0000 |            | `[AB]` から A にロード             |

スタック レベル`A`およびBであらわされるアドレスから値を読み込み、スタック レベル`A`に格納します。また、もともとスタックにあった値は押し下がります。アドレスのMSBはレベル`B`です。

例 :

```asm
LI #0xF
; stack A B C
;       F x x
LI #0x0
; stack A B C
;       0 F x
LM
; Loads data from address 0xF0 
; stack   A    B C
;       [0xF0] 0 F
```

#### `LD`

| 命令     | Opc  | Opr  | フラグ変化 | 説明                               |
| -------- | ---- | ---- | ---------- | ---------------------------------- |
| `LD r`   | 1001 | r    |            | r から A にロード                  |

`r`であらわされるアドレスの値から値を読み込み、スタック レベル`A`に格納します。また、もともとスタックにあった値は押し下がります。

例 :

```asm
LI #3
; stack A B C
;       3 x x
SA r3
; stores 3 to 0x03(r3)
LD r3
; loads data from 0x03(r3)
; stack A B C
;       3 3 x
```

#### `LI`

| 命令     | Opc  | Opr  | フラグ変化 | 説明                               |
| -------- | ---- | ---- | ---------- | ---------------------------------- |
| `LI #i`  | 1010 | i    | ※           | 即値を A にロード                  |

命令中の`#i`であらわされる即値をスタック レベル`A`に格納します。また、もともとスタックにあった値は押し下がります。`#i`には10進数のほかに16進数、2進数リテラルが使用できます。

例 :

```asm
LI #0xF
; stack A B C
;       F x x
LI #0x0
; stack A B C
;       0 F x
```

### システム制御

#### `JP`, `NP`

| 命令    | Opc  | Opr  | 説明                           |
| ------- | ---- | ---- | ------------------------------ |
| `JP`    | 1110 | 0000 | `[ABC]` へジャンプ             |
| `JP C`  | 1110 | 0010 | C フラグが 1 のとき`[ABC]` へジャンプ    |
| `JP NC` | 1110 | 0011 | C フラグが 0 のとき`[ABC]` へジャンプ    |
| `JP Z`  | 1110 | 0100 | Z フラグが 1 のとき`[ABC]` へジャンプ    |
| `JP NZ` | 1110 | 0101 | Z フラグが 0 のとき`[ABC]` へジャンプ    |
| `NP`    | 1110 | 0001 | 何もしない                     |

- `NP` は何もしません。
- `JP` 系命令は条件に応じて PC を更新します。

- ジャンプする場合、PCにスタック レベル`A`,`B`,`C`であらわされる値を格納し、次の命令はスタック レベル`A`,`B`,`C`であらわされる値のものを読み込みます。MSBはレベル`C`です。
- ジャンプしない場合、PCは通常通りインクリメントされます。

例 :

```asm
LI #0x0
; stack A B C
;       0 x x
LI #0x1
; stack A B C
;       1 0 x
LI #0x4
; stack A B C
;       4 1 0
JP
; 0x014にジャンプ(次の命令は ROM:0x014 よりフェッチされます。)
```

## チュートリアル: LED点滅（lchika.asm）

対象ファイル: `extra/lchika.asm`

```asm
np
li #0xF
li #0xF
li #0
sm
li #0
li #0xF
li #0
sm
li #0
li #0
li #0x1
jp
```

流れ:

1. `li #0xF`, `li #0xF`, `li #0` で書き込み先アドレスと値をスタックに準備
2. `sm` でメモリマップドI/Oへ出力値を書き込む
3. 別の値を書き込んで LED ON/OFF を切り替える
4. 最後に `jp` で先頭に戻り、点滅を繰り返す

このサンプルは「ロード」「ストア」「ジャンプ」の最小構成で、最初に動かす確認用として使えます。

行単位の解説:

1. `np`: 初期NOP
2. `li #0xF`: アドレス上位側の準備
3. `li #0xF`: アドレス下位側の準備
4. `li #0`: 出力データ（例: OFF）
5. `sm`: I/Oへ書き込み
6. `li #0`: 次の遷移用データ準備
7. `li #0xF`: 同一I/O領域指定
8. `li #0`: 出力データ再設定
9. `sm`: I/Oへ再書き込み
10. `li #0`: ジャンプ先下位準備
11. `li #0`: ジャンプ先中位準備
12. `li #0x1`: ジャンプ先上位準備
13. `jp`: 先頭側へ戻る

注記:

- 実機で点滅しない場合は、I/Oデコード範囲と極性（アクティブHigh/Low）を最初に確認してください。

## トラブルシューティング

- I/O アドレスの割り当てが回路と一致しているか確認する
- `JP` の飛び先アドレス（スタック内容）が意図どおりか確認する
- フラグ条件ジャンプを使う場合、直前命令の C/Z 更新を確認する
- `LD`/`LI` 後のスタック押し下がりで、想定外の値が B/C に残っていないか確認する
- HC4E では 2段スタック前提のため、C段前提のコードを書いていないか確認する

## 既知のエラッタ

- HC4 Rev1
  - `Z`フラグが壊れている
    Zフラグの制御線が誤ったところにつながっているのが原因。  
    対処法:`Z`フラグを使用しない
    Rev2で修正済み
  - `C`フラグが`LI`命令で変更される  
    イミディエイトの制御線と`C`フラグの制御線が共有されていることが原因。  
    対処法:`C`フラグを使用する加算の場合、直前で`LI`命令を使わず、いったんレジスタにロードして`LD`命令を使用する。  
    Rev2で修正済み
  - レベルCのLSBとMSBが逆になっている  
    対処法: なし(出力のみを行う場合、データバスの結線を逆にすることで対処可能)  
    Rev2で修正済み
 
- HC4 Rev2  
  - `SU`, `XR`命令が使用できない  
    命令デコーダ部の結線ミスが原因。修正可能。  
    対処法: `SU`, `XR`命令を使用しない  
    Rev3で修正済み  
